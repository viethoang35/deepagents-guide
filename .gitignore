# Secrets
.env

# Python
__pycache__/
*.pyc
.pytest_cache/
*.egg-info/
.venv/

# Vendor clones (re-clone via the commands in README, not committed to this repo)
vendor-deepagents/
vendor-nemoclaw/

# State/checkpoints generated by running scripts (no need to commit)
workspace/*.db

# Scratch git repos bootstrapped by demo scripts (nested .git, not meant to be committed)
workspace/pr-review-repo/
